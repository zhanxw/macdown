# GitHub Style Generator

This tool generates the GitHub style with official GitHub style package. Node.js 18 or later and npm are required. The generator uses Dart Sass, which runs natively on both Apple Silicon and Intel Macs. To update the style, bump the name in `Makefile`, and run:

```bash
npm ci
npm update primer-markdown
make
```
